var ids = [];
var arr = $('.g-drop');
arr.each(function() {
    if ($(this).attr('style') == 'display: block;') {
        ids.push($(this).attr('id'));
    }
});

$(".my-games").html("<%= j(render partial: 'my_games/hub', locals: { collection: @collection }) %>");
$('#r-q').text(<%= raw query %>);
$('#r-s').text('<%= sort %>');

ids.forEach(id => $('#' + id).show());

moreInfo();
