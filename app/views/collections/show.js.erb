$("li.active").removeClass('active');
$("#mc-<%= @collection.id %>").addClass('active');

var refresh = eval('<%= @refresh %>')
if (refresh) {
  var ids = [];
  var arr = $('.g-drop');
  arr.each(function() {
      if ($(this).attr('style') == 'display: block;') {
          ids.push($(this).attr('id'));
      }
  });
}

$("#big-c").html("<%= j(render partial: 'my_games', locals: { collection: @collection }) %>");

if (refresh) {
  ids.forEach(id => $('#' + id).show());
}

// Read cookies and define partial
//
//

// Render proper partial and corresponding functions
//
//

// Function for changing views


changeView("<%= j(render partial: 'my_games/covers') %>");
filter();
moreInfo();
