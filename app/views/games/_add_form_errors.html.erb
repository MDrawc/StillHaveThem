<% if errors.any? %>
  <span id="add-form-errors" class="blink_me"><%= render 'static_pages/warning' %></span>
  <span class="add-acc-errors blink_me"><%= @errors.join(', ') %></span>
<% else %>
<% end %>
