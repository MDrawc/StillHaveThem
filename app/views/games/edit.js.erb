var game_id = '<%= @game_id %>'
var new_game_id = '<%= @new_game_id %>'
var msg = '<%= @message %>';
var msg_pos = 'top-right';
var success = <%= @errors.empty? %>;

// Notifications position depends on offcanvas position
if ($('.uk-offcanvas-container').length) {
    var offc_class = $('.uk-offcanvas-container').attr('class');

    if (offc_class.includes('uk-offcanvas-flip')) {
        msg_pos = 'top-left';
    }
}

if (msg) {
    UIkit.notification({
        message: $('<div/>').html(msg).text(),
        pos: msg_pos,
        timeout: 5000
    });
}

if (success) {

    reopen = $('#g-drop-' + game_id).attr('style') == 'display: block;';
    var data = { type: 'refresh' };

    if ($('#my-search').text().length != 0) {
        search = $('#my-search').text().split(',');
        data['q'] = { name_dev_cont: search[0], plat_cont: search[1] }
    }

    if ($('.pr em.current').length != 0) {
        var current_page = $('.pr em.current').text();
        data['page'] = current_page;
    }

    $.ajax({
        url: '/collections/<%= @collection.id %>',
        data: data,
        complete: function(data) {
            if (reopen) {
                $('#g-drop-' + new_game_id).show();
            }
        }
    });

} else {
    $("#edit-form-" + game_id + " .add-form-errors").html("<%= escape_javascript(render partial: 'add_form_errors', locals: { errors: @errors }) %>");
    resetFormErrors('edit', game_id);
}
