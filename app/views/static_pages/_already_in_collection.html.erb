<p>"<%= game_name %>" games already in "<%= collection.custom_name || collection.name %>":</p>
<ul>
  <% collection.games.where(igdb_id: igdb_id).each do |game| %>
  <li><%= game.platform_name %> (<%= game.physical ? 'Physical' : 'Digital' %>)
    <%= link_to '', remove_s_url(game_id: game.id, collection_id: collection.id, id_type: 'game_id'),
    class: 'uk-icon-link', method: :delete, remote: true, 'uk-icon' => 'trash' %>
  </li>
  <% end %>
</ul>
