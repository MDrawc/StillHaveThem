<% if logged_in? %>
  <script>
  <% if @home_id %>
    var url = '/collections/<%= @home_id %>'
  <% else %>
    var url = '/search'
  <% end %>
  $.ajax({ url: url });
  </script>
<% else %>
  <%= render 'not_logged' %>
<% end %>
