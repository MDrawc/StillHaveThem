$("#search-errors").replaceWith("<%= escape_javascript(render :partial => 'search_errors') %>");

if (<%= !@inquiry.errors.any? %>) {
  $("#search-results").html("<%= escape_javascript(render :partial => 'results') %>");
}
