<% if logged_in? %>
  <div id="coll-modal" class="uk-flex-top" uk-modal>
    <div class="uk-modal-dialog uk-modal-body uk-margin-auto-vertical">
      <button class="uk-modal-close-default" type="button" uk-close></button>
      <div class="modal-header">
        <p class="header"><span id="coll-action">Create</span> custom collection<span class='add-coll-icon'><%= svg 'shelf_icon' %></span></p>
      </div>
      <div class="modal-content"></div>
    </div>
  </div>
<% end %>

<div id="f-modal" uk-modal>
    <div class="uk-modal-dialog uk-modal-body">
        <button id="f-modal-close" class="uk-modal-close-default f-modal-close" type="button" uk-close></button>
        <div class="modal-content">
            <span class="loading">Loading</span>
        </div>
    </div>
</div>
